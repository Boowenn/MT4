# QuantGod Vibe Coding — Iterate Strategy

Revise the previous BaseStrategy implementation based on user feedback and optional backtest metrics.
Keep the same safety constraints and preserve the BaseStrategy interface.
Return only Python code.
